% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semiparametric_sft.R
\name{mic.bayes}
\alias{mic.bayes}
\title{Extract the hierarchical posterior MIC from an OR capacity model.}
\usage{
mic.bayes(object, ...)
}
\arguments{
\item{object}{A fitted \code{sft_bayes} object, or a canonical SFT data
frame to fit with \code{\link[=semiparametricSFT.bayes]{semiparametricSFT.bayes()}}.}

\item{...}{Passed to \code{\link[=semiparametricSFT.bayes]{semiparametricSFT.bayes()}} when \code{object} is a data frame. A
\code{salience_split} is required to identify the cells and defaults to \code{TRUE}.}
}
\value{
A list with \code{summary}, subject-to-population \code{contrasts}, per-level
\code{draws}, and metadata. Summary rows contain posterior means,
highest-density intervals, and sign probabilities.
}
\description{
The mean interaction contrast MIC = mean(LL) - mean(LH) - mean(HL) + mean(HH)
is the signed area under the survivor interaction contrast. It is computed
draw by draw at the subject, population (typical-subject parameter),
population_finite_mean, and new_subject levels, together with
subject-to-population contrasts.
It is only identified from a salience-split fit.
}
\details{
Positive MIC values support an over-additive architecture, such as parallel
self-terminating or coactive processing. Negative values support a
parallel-exhaustive architecture. Values near zero are consistent with an
additive serial architecture.
}
\seealso{
\code{\link[=semiparametricSFT.bayes]{semiparametricSFT.bayes()}}, \code{\link[=sic.bayes]{sic.bayes()}}, \code{\link[=mic.test]{mic.test()}}
}
