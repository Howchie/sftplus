% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes_sensitivity.R
\name{spike_slab}
\alias{spike_slab}
\title{Per-participant spike-and-slab test of the SFT null.}
\usage{
spike_slab(
  fit,
  w_shape1 = 1,
  w_shape2 = 1,
  ndraws = 10000L,
  burnin = 1000L,
  thin = 1L,
  chains = 4L,
  seed = NULL,
  hdi = NULL
)
}
\arguments{
\item{fit}{A fitted object carrying \code{refit_data}; see
\code{\link{prior_sensitivity}}.}

\item{w_shape1, w_shape2}{Beta prior on the inclusion rate \eqn{w}.  The
default \code{Beta(1, 1)} is uniform and gives a prior inclusion probability
of one half.}

\item{ndraws, burnin, thin, chains, seed}{Sampler controls.}

\item{hdi}{Interval mass for the slab-effect summaries.}
}
\value{
A list with a per-participant \code{summary} (inclusion probability,
Bayes factor, and posterior effect), a \code{population} summary of
\eqn{w}, \eqn{\mu}, and \eqn{\tau}, the \code{draws}, and MCMC
\code{diagnostics}.
}
\description{
Tests which participants depart from the benchmark. Each participant's effect is either
exactly zero (the spike) or drawn from the population slab, and the posterior
inclusion probability \eqn{P(z_i = 1)} measures support for the slab.
}
\details{
Each participant also receives a Bayes factor from the change in inclusion odds
relative to the prior inclusion probability, which is the prior mean of
\eqn{w}. Estimating \eqn{w} shares information about inclusion across
participants and discounts isolated departures in an otherwise null sample.
}
\seealso{
\code{\link{prior_sensitivity}}, \code{\link{capacityGroup.bayes}}
}
