% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{sft_data_to_rt}
\alias{sft_data_to_rt}
\title{Convert canonical row-wise SFT data to RT/CR list input.}
\usage{
sft_data_to_rt(
  data,
  Condition = NULL,
  Subject = NULL,
  stopping.rule = c("OR", "AND", "STST"),
  by_subject = c("auto", "always", "never"),
  include_nn = FALSE
)
}
\arguments{
\item{data}{A data frame containing \code{Subject}, \code{RT}, \code{Correct}, \code{Channel1},
and \code{Channel2}; \code{Condition} is optional when only one condition is used.
The aliases \code{subjects}, \code{rt}, and \code{LogicalRule} are accepted for
\code{Subject}, \code{RT}, and \code{Condition}, respectively.}

\item{Condition}{Optional single condition value to retain.  If omitted,
the data must contain one condition (or no \code{Condition} column).}

\item{Subject}{Optional subject value or values to retain.}

\item{stopping.rule}{One of \code{"OR"}, \code{"AND"}, or \code{"STST"}.}

\item{by_subject}{\code{"auto"} returns a flat list for one subject and nested
lists for multiple subjects; \code{"always"} or \code{"never"} forces either form.}

\item{include_nn}{Include the both-off \code{NN} cell for OR/AND conversion.}
}
\value{
A list with \code{RT} and matching \code{CR} lists.  Metadata are stored as
attributes, including selected subjects, condition, and dropped rows.
}
\description{
Converts a trial-level data frame to one response-time vector and one
correctness vector per experimental cell. For OR and AND stopping rules it
returns AB, A, and B, using positive channel values as target-present. For
STST it returns
context and target cells using the package's signed-channel convention.
With multiple subjects, the default result is nested by subject so it can
be passed to \code{capacityGroup.bayes()}; select one subject or use
\code{by_subject = "never"} for single-subject functions.
}
\details{
Rows with both channels off are omitted from OR and AND output unless
\code{include_nn = TRUE}. Under the STST convention, context rows have one
positive and one negative channel; target rows have one nonzero,
non-negative channel.
}
\examples{
one <- sft_data_to_rt(dots, Condition = "OR", Subject = "S1")
names(one$RT)

group <- sft_data_to_rt(dots, Condition = "OR")
length(group$RT)
}
