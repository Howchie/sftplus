% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mic_bayes.R
\name{micGroup.bayes}
\alias{micGroup.bayes}
\title{Hierarchical Bayesian relative mean interaction contrast (MIC).}
\usage{
micGroup.bayes(
  inData,
  Condition = NULL,
  Subject = NULL,
  correct = TRUE,
  var_method = c("analytic", "bootstrap"),
  n_boot = 2000L,
  ndraws = 10000L,
  prior_shape = 2,
  prior_rate = 0.01,
  seed = NULL,
  hdi = 0.94,
  prior_mean = 0,
  prior_sd = 0.1,
  burnin = 1000L,
  thin = 1L,
  chains = 4L,
  rope = NULL,
  method = c("InvGamma", "HalfNormal", "Centered"),
  prior_tau_sd = 0.1,
  adapt_delta = 0.95,
  max_treedepth = 12L,
  stan_control = list(),
  tau2_prior_shape = NULL,
  tau2_prior_rate = NULL
)
}
\arguments{
\item{inData}{A canonical SFT data frame (\code{Subject}, \code{RT},
\code{Correct}, \code{Channel1}, \code{Channel2}, optional \code{Condition}),
with \code{subjects}, \code{rt}, and \code{LogicalRule} accepted as column aliases;
a nested list of per-subject \code{HH}/\code{HL}/\code{LH}/\code{LL} RT
lists, or a flat single-subject \code{HH}/\code{HL}/\code{LH}/\code{LL} list.}

\item{Condition, Subject}{Optional selectors for data-frame input.
\code{Condition = NULL} fits all available conditions. Multiple-condition fits
return one population mean per condition and their pairwise contrasts.
Participants are not linked across conditions.}

\item{correct}{Keep only correct trials (the conditional-correct MIC).}

\item{var_method}{\code{"analytic"} (delta-method, the default) or
\code{"bootstrap"} (within-cell resampling) sampling variance of \eqn{\rho}.}

\item{n_boot}{Bootstrap replicates when \code{var_method = "bootstrap"}.}

\item{ndraws, burnin, thin, chains, seed, hdi}{Sampler and summary controls.}

\item{prior_shape, prior_rate}{Inverse-Gamma prior on \eqn{\tau_\rho^2} for the
\code{"InvGamma"} sampler.}

\item{prior_mean, prior_sd}{Normal prior on the population relative MIC
\eqn{\mu_\rho}.  Because \eqn{\rho} is dimensionless, \code{prior_sd = 0.10}
encodes population MICs within roughly \eqn{\pm 20\%} of mean RT.}

\item{rope}{Optional proportional region of practical equivalence on
\eqn{\rho}; for example, \code{0.025} treats interactions under 2.5\\% of mean RT as
negligible.}

\item{method}{One of \code{"InvGamma"}, \code{"HalfNormal"}, or
\code{"Centered"}.}

\item{prior_tau_sd}{Half-Normal prior scale on \eqn{\tau_\rho} for the Stan
samplers.}

\item{adapt_delta, max_treedepth, stan_control}{Stan sampler controls.}

\item{tau2_prior_shape, tau2_prior_rate}{Back-compatible aliases for
\code{prior_shape} and \code{prior_rate}.}
}
\value{
A list mirroring \code{\link{capacityGroup.bayes}} with an added
\code{estimand} label. \code{posterior_probability} reports
over-additive / under-additive / additive probabilities for the population
and each participant. Multiple-condition fits also contain
\code{condition_contrasts}.
}
\description{
Estimates the salience mean interaction contrast and partially pools it
across participants. The estimand is the relative MIC,
\eqn{\rho_i = \mathrm{MIC}_i / M_i}, where \eqn{\mathrm{MIC}_i =
\bar T_{HH} - \bar T_{HL} - \bar T_{LH} + \bar T_{LL}} and \eqn{M_i} is the
equal-weight grand-mean RT of the four cells. Dividing by \eqn{M_i} removes
multiplicative speed differences between participants while preserving the
sign and zero point of the interaction contrast. A region of practical
equivalence is therefore expressed as a proportion of mean RT.
}
\details{
Cells are taken from the salience 2x2 defined by \code{Channel1} and
\code{Channel2} (2 = High, 1 = Low). A single participant is fitted with the
analytic normal-normal posterior; two or more are pooled hierarchically with
the conjugate Gibbs (\code{"InvGamma"}) or Stan samplers used by
\code{\link{capacityGroup.bayes}}.
}
\seealso{
\code{\link{capacityGroup.bayes}}, \code{\link{mic.test}},
\code{\link{mic.bayes}}
}
